现在每一幅图的每个初始信念节点随机从高, 中, 低, 无三中训练节点中选择

高 [0.9, 0.05, 0.05]
中 [0.7, 0.15, 0.15]
低 [0.5, 0.25, 0.25]
无 [0.3, 0.3, 0.3]

现在初始化的 n-1 个节点不全为中，高，低，无, 而是每个节点是随机的

n = {5, 6}

每幅图按照前文这样说的遍历方法，历出所有的可能的结构加上初始化的可能, 一共会有

对于 n = 5 的图，有 17 种可能的结构(非同构, 三色可解, 全连接)
对于 n = 6 的图，有 173 种可能的结构(非同构, 三色可解, 全连接)

17 * 5 * (4^4) + 173 * 6 * (5 ^ 4) = 17 * 5 * 256 + 173 * 6 * 625 = 21500 + 626250 = 647750

在此基础上，按照两条规则去重, 
去掉同构性的图（比如说对于同一幅图来说，选择不同的初始节点，可能实际上在拓扑上是一样的，因为图是对称的）

初始化节点，相邻节点，最高权重的颜色属性是不一样的（ps：对于等级为无的节点来说，无所谓，没有忌讳）

对应这 647750(不同结构 * 不同初始化) 幅图

筛选出 30 幅指标最好的图
指标:1. agentA0-A4 的KL差值
2. 
